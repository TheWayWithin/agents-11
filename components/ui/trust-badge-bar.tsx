'use client';

import { Shield, Star, Users, Award, Clock, CreditCard } from 'lucide-react';

interface TrustBadgeBarProps {
  className?: string;
  variant?: 'header' | 'footer' | 'pricing';\n  showAnimation?: boolean;\n}\n\ninterface TrustBadge {\n  icon: React.ReactNode;\n  text: string;\n  subtext?: string;\n  color?: string;\n}\n\nconst trustBadges: TrustBadge[] = [\n  {\n    icon: <Shield className=\"h-4 w-4\" />,\n    text: 'Enterprise Security',\n    subtext: 'SOC2 & GDPR Compliant',\n    color: 'text-green-600',\n  },\n  {\n    icon: <Star className=\"h-4 w-4\" />,\n    text: '4.9/5 Rating',\n    subtext: 'From 2,847 reviews',\n    color: 'text-yellow-600',\n  },\n  {\n    icon: <Users className=\"h-4 w-4\" />,\n    text: '2,847+ Users',\n    subtext: 'Active entrepreneurs',\n    color: 'text-blue-600',\n  },\n  {\n    icon: <Award className=\"h-4 w-4\" />,\n    text: '30-Day Guarantee',\n    subtext: 'Money back promise',\n    color: 'text-purple-600',\n  },\n  {\n    icon: <Clock className=\"h-4 w-4\" />,\n    text: '24/7 Support',\n    subtext: 'Always here to help',\n    color: 'text-indigo-600',\n  },\n  {\n    icon: <CreditCard className=\"h-4 w-4\" />,\n    text: 'Secure Payments',\n    subtext: 'Stripe protected',\n    color: 'text-emerald-600',\n  },\n];\n\nconst headerBadges = trustBadges.slice(0, 4);\nconst pricingBadges = [trustBadges[0], trustBadges[1], trustBadges[3], trustBadges[5]];\n\nexport function TrustBadgeBar({\n  className = '',\n  variant = 'header',\n  showAnimation = true,\n}: TrustBadgeBarProps) {\n  const getBadges = () => {\n    switch (variant) {\n      case 'header':\n        return headerBadges;\n      case 'pricing':\n        return pricingBadges;\n      case 'footer':\n        return trustBadges;\n      default:\n        return trustBadges;\n    }\n  };\n\n  const getContainerClasses = () => {\n    switch (variant) {\n      case 'header':\n        return 'bg-gray-50 border-b border-gray-200 py-2';\n      case 'pricing':\n        return 'bg-white border border-gray-200 rounded-lg p-4 shadow-sm';\n      case 'footer':\n        return 'bg-gray-900 text-white py-4';\n      default:\n        return 'bg-gray-50 py-3';\n    }\n  };\n\n  const badges = getBadges();\n\n  return (\n    <div className={`${getContainerClasses()} ${className}`}>\n      <div className=\"container mx-auto px-4\">\n        <div className=\"flex flex-wrap items-center justify-center gap-6 md:gap-8\">\n          {badges.map((badge, index) => (\n            <div\n              key={index}\n              className={`flex items-center space-x-2 text-sm ${\n                showAnimation ? 'animate-fade-in' : ''\n              }`}\n              style={{\n                animationDelay: showAnimation ? `${index * 0.2}s` : '0s',\n              }}\n            >\n              <div className={badge.color || 'text-gray-600'}>\n                {badge.icon}\n              </div>\n              <div>\n                <div className={`font-semibold ${\n                  variant === 'footer' ? 'text-white' : 'text-gray-900'\n                }`}>\n                  {badge.text}\n                </div>\n                {badge.subtext && (\n                  <div className={`text-xs ${\n                    variant === 'footer' ? 'text-gray-300' : 'text-gray-500'\n                  }`}>\n                    {badge.subtext}\n                  </div>\n                )}\n              </div>\n            </div>\n          ))}\n        </div>\n      </div>\n    </div>\n  );\n}\n\n// Compact version for mobile\nexport function CompactTrustBadges({ className = '' }: { className?: string }) {\n  return (\n    <div className={`flex items-center justify-center space-x-4 text-xs text-gray-600 ${className}`}>\n      <div className=\"flex items-center space-x-1\">\n        <Shield className=\"h-3 w-3 text-green-600\" />\n        <span>Secure</span>\n      </div>\n      <div className=\"flex items-center space-x-1\">\n        <Star className=\"h-3 w-3 text-yellow-600\" />\n        <span>4.9/5</span>\n      </div>\n      <div className=\"flex items-center space-x-1\">\n        <Users className=\"h-3 w-3 text-blue-600\" />\n        <span>2,847+ users</span>\n      </div>\n      <div className=\"flex items-center space-x-1\">\n        <Award className=\"h-3 w-3 text-purple-600\" />\n        <span>30-day guarantee</span>\n      </div>\n    </div>\n  );\n}"